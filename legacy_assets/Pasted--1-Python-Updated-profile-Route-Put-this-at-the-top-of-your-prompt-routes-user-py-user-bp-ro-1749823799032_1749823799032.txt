✅ 1. Python: Updated /profile Route
Put this at the top of your prompt:
# routes/user.py
@user_bp.route('/profile', methods=['GET', 'POST'])
@login_required
def profile():
    all_tournaments = Tournament.query.order_by(Tournament.name).all()

    if request.method == 'POST':
        current_user.first_name = request.form.get('first_name')
        current_user.last_name = request.form.get('last_name')
        current_user.notifications = 'notifications' in request.form

        attended_ids = request.form.getlist('attended_tournaments[]')
        wishlist_ids = request.form.getlist('wishlist_tournaments[]')

        db.session.query(UserPastTournament).filter_by(user_id=current_user.id).delete()
        db.session.query(UserWishlistTournament).filter_by(user_id=current_user.id).delete()

        for tid in attended_ids:
            db.session.add(UserPastTournament(user_id=current_user.id, tournament_id=int(tid)))
        for tid in wishlist_ids:
            db.session.add(UserWishlistTournament(user_id=current_user.id, tournament_id=int(tid)))

        db.session.commit()
        flash("Your profile has been updated.", "success")
        return redirect(url_for('user.profile'))

    past_ids = [t.tournament_id for t in current_user.past_tournaments]
    wishlist_ids = [w.tournament_id for w in current_user.wishlist]

    return render_template(
        'user/profile.html',
        user=current_user,
        past_tournaments=all_tournaments,
        attended_ids=past_ids,
        wishlist_ids=wishlist_ids,
    )
✅ 2. Jinja Template: templates/user/profile.html
Paste this after the route:
<!-- templates/user/profile.html -->
{% extends "layout.html" %}
{% block content %}

<div class="mx-auto max-w-3xl px-4 py-12">
  <h1 class="font-serif text-3xl font-medium">Your Profile</h1>
  <p class="mt-2 text-muted-foreground">Update your preferences and manage your CourtSide Club experience.</p>

  <form method="POST" class="space-y-10 mt-10">
    {{ csrf_token() }}

    <!-- Account Info -->
    <section>
      <h2 class="font-serif text-2xl font-medium">Account Information</h2>
      <div class="mt-6 grid gap-6 md:grid-cols-2">
        <div>
          <label for="first_name" class="block mb-1 font-medium">First Name</label>
          <input name="first_name" value="{{ user.first_name }}" class="w-full border border-gray-300 rounded p-2" />
        </div>
        <div>
          <label for="last_name" class="block mb-1 font-medium">Last Name</label>
          <input name="last_name" value="{{ user.last_name }}" class="w-full border border-gray-300 rounded p-2" />
        </div>
        <div class="md:col-span-2">
          <label class="block mb-1 font-medium">Email</label>
          <input value="{{ user.email }}" disabled class="w-full bg-gray-100 text-gray-500 rounded p-2" />
        </div>
        <div class="md:col-span-2 flex justify-between items-center">
          <div>
            <label for="notifications" class="font-medium">Email Notifications</label>
            <p class="text-sm text-gray-500">Receive updates about tournaments and events</p>
          </div>
          <input type="checkbox" name="notifications" {% if user.notifications %}checked{% endif %} />
        </div>
      </div>
    </section>

    <hr class="my-6" />

    <!-- Events I've Been To -->
    <section>
      <h2 class="font-serif text-2xl font-medium mb-2">Events I’ve Been To</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-2">
        {% for t in past_tournaments %}
        <label class="flex items-center space-x-2">
          <input type="checkbox" name="attended_tournaments[]" value="{{ t.id }}" {% if t.id in attended_ids %}checked{% endif %} />
          <span>{{ t.name }}</span>
        </label>
        {% endfor %}
      </div>
    </section>

    <hr class="my-6" />

    <!-- Bucket List -->
    <section>
      <h2 class="font-serif text-2xl font-medium mb-2">Bucket List Tournaments</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-2">
        {% for t in past_tournaments %}
        <label class="flex items-center space-x-2">
          <input type="checkbox" name="wishlist_tournaments[]" value="{{ t.id }}" {% if t.id in wishlist_ids %}checked{% endif %} />
          <span>{{ t.name }}</span>
        </label>
        {% endfor %}
      </div>
    </section>

    <!-- Invite a Friend -->
    <section class="bg-gray-100 p-4 rounded">
      <div class="flex justify-between items-center flex-col md:flex-row gap-4">
        <div>
          <h3 class="font-serif text-xl font-medium">Invite a Friend</h3>
          <p class="text-sm text-gray-600">Share your love for tennis with friends</p>
        </div>
        <a href="/invite" class="bg-green-700 hover:bg-green-800 text-white px-4 py-2 rounded">Invite via Text</a>
      </div>
    </section>

    <!-- Save Button -->
    <div class="text-center md:text-left">
      <button type="submit" class="mt-6 bg-green-700 hover:bg-green-800 text-white px-6 py-3 rounded font-medium">
        Save My Profile
      </button>
    </div>

    <!-- Change password -->
    <p class="mt-6 text-sm text-center text-green-800 hover:underline">
      <a href="/reset">Want to change your password?</a>
    </p>
  </form>
</div>

{% endblock %}
✅ 3. Instructions for Replit Agent
At the end of the message, paste this:
Do not finish until you’ve fully tested that:
The profile form loads existing user data
Updates persist correctly to the database
Redirect works after save
Tournament checkboxes reflect accurate selection
Layout works on both mobile and desktop preview