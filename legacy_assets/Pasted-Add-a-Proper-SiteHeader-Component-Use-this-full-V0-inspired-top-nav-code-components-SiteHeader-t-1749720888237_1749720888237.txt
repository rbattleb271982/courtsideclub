Add a Proper SiteHeader Component
Use this full V0-inspired top nav code:
// components/SiteHeader.tsx
"use client"

import Link from "next/link"
import { Button } from "@/components/ui/button"
import { Menu } from "lucide-react"

export function SiteHeader() {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-white/90 backdrop-blur-sm border-b border-neutral-100">
      <div className="container flex h-16 items-center justify-between px-4 md:px-8 lg:px-12">
        <div className="flex items-center gap-2">
          <Link href="/" className="flex items-center gap-2">
            <div className="rounded-md bg-sage-600 p-1.5">
              <span className="text-white font-semibold text-sm">C</span>
            </div>
            <span className="font-serif text-lg tracking-tight">CourtSide Club</span>
          </Link>
        </div>
        <nav className="hidden md:flex items-center gap-6">
          <Link href="#" className="text-sm font-medium hover:text-sage-700 transition-colors">Tournaments</Link>
          <Link href="#" className="text-sm font-medium hover:text-sage-700 transition-colors">Community</Link>
          <Link href="#" className="text-sm font-medium hover:text-sage-700 transition-colors">Stories</Link>
          <Link href="#" className="text-sm font-medium hover:text-sage-700 transition-colors">About</Link>
        </nav>
        <div className="flex items-center gap-4">
          <Button variant="outline" size="sm" className="hidden md:inline-flex rounded-full px-4">Sign In</Button>
          <Button size="sm" className="rounded-full px-4 bg-sage-600 hover:bg-sage-700 text-white">Join Club</Button>
          <Button variant="ghost" size="sm" className="md:hidden">
            <Menu className="h-5 w-5" />
          </Button>
        </div>
      </div>
    </header>
  )
}
✅ Then import and render it in your homepage layout:
// pages/index.tsx (or wherever your main layout is)
import { SiteHeader } from "@/components/SiteHeader"
import { Hero } from "@/components/Hero"
// (import other sections here)

export default function Home() {
  return (
    <>
      <SiteHeader />
      <main>
        <Hero />
        {/* other sections */}
      </main>
    </>
  )
}
🧪 After adding:
Make sure your global layout has enough top padding (pt-16 or similar) to avoid overlap with fixed header
Adjust your container spacing if needed (like max-w-7xl mx-auto px-6 lg:px-8)