Got it! Here's the updated version with your **custom password input request** added to the top — still written in Replit-ready language so you can copy and paste the whole thing directly:

---

### 📋 COURTSIDECLUB – TOURNAMENT FLOW + ACCOUNT SETUP FIXES (FOR REPLIT)

---

#### ✅ 0. Fix Registration Form (Login Page)

- When users register, they should be able to:
  - Enter **first name**
  - Enter **last name**
  - Enter **email**
  - Enter **password manually**
- 🔥 Remove the message about generating a secure password for them — that should not happen.
- ✅ Passwords should be user-defined at sign-up, and stored in Replit DB under `user['password']`

---

#### ✅ 1. Update the Tournaments Page (`/tournaments`)

- Each tournament should have one checkbox labeled **“I’m attending”**
- When a user checks a tournament and clicks **Save**, redirect them to:  
  `/tournament/<tournament_id>`
- If a user has already selected sessions for a tournament, display a ✅ next to that tournament name
- **Remove all day/session selection UI** from this page — it now lives on the individual tournament page

---

#### ✅ 2. Add Logic for Access Control on `/tournament/<id>`

- If a user accesses this page without first marking “I’m attending”:
  - Show this message:  
    `"You must first mark yourself as attending from the tournaments page to select sessions."`
  - Include a link to go back to `/tournaments`

---

#### ✅ 3. On `/tournament/<id>` (Individual Tournament Page):

- Display sessions grouped by day using the `sessions` array  
  (e.g., Day 1 – Day / Night)
- Let users check boxes for sessions they're attending

- Under that, add a section:
  `"Open to meeting?"`  
  With two radio buttons:
  - Yes
  - No  
  ➤ **Required:** they must select one before saving

- One **Save** button at the bottom of the page:
  - Saves:
    - `raised_hand[tournament_id][day] = ["Day", "Night"]`
    - `meeting_pref[tournament_id] = "Yes"` or `"No"`
  - If all session checkboxes are unchecked:
    - Remove the tournament from `raised_hand`
    - Uncheck the “I’m attending” box on the tournaments overview page

---

Let me know when you want to move on to building the HTML or Flask backend for this — I’ve got you.