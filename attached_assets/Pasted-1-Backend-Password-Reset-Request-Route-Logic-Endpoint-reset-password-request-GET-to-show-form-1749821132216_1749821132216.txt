1. Backend: Password Reset Request Route & Logic
Endpoint: /reset_password/request (GET to show form, POST to submit email)
Generate a secure reset token linked to the user
Send the reset email with the token URL (using the new password reset email template)
Save token with expiration in DB (e.g., 1 hour validity)
Sample Flask snippet (simplified):
from flask import request, render_template, url_for, redirect, flash
from itsdangerous import URLSafeTimedSerializer
from yourapp.models import User
from yourapp.services.email import send_password_reset_email

serializer = URLSafeTimedSerializer(app.config['SECRET_KEY'])

@app.route('/reset_password/request', methods=['GET', 'POST'])
def reset_password_request():
    if request.method == 'POST':
        email = request.form['email']
        user = User.query.filter_by(email=email).first()
        if user:
            token = serializer.dumps(user.email, salt='password-reset-salt')
            reset_url = url_for('reset_password_confirm', token=token, _external=True)
            send_password_reset_email(user.email, user.first_name, reset_url)
        flash('If your email is registered, you will receive a password reset link.')
        return redirect(url_for('login'))
    return render_template('reset_password_request.html')
2. Backend: Password Reset Confirmation Route & Logic
Endpoint: /reset_password/confirm/<token> (GET to show form, POST to submit new password)
Validate token with expiration
Validate password strength
Update user password securely (hashing)
Redirect to login with success message
Example snippet:
from flask import request, render_template, flash, redirect, url_for
from werkzeug.security import generate_password_hash

@app.route('/reset_password/confirm/<token>', methods=['GET', 'POST'])
def reset_password_confirm(token):
    try:
        email = serializer.loads(token, salt='password-reset-salt', max_age=3600)
    except Exception:
        flash('Invalid or expired token')
        return redirect(url_for('reset_password_request'))
    
    user = User.query.filter_by(email=email).first_or_404()

    if request.method == 'POST':
        password = request.form['password']
        # Add password validation logic here
        if len(password) < 8:
            flash('Password must be at least 8 characters.')
            return render_template('reset_password_confirm.html', token=token)
        
        user.password_hash = generate_password_hash(password)
        db.session.commit()
        flash('Your password has been updated.')
        return redirect(url_for('login'))
    
    return render_template('reset_password_confirm.html', token=token)
3. Email Template and Sending Logic
Email contains a clear CTA button linking to /reset_password/confirm/<token>
Use the new enhanced password reset email design we crafted
Implement the send function in services/email.py:
def send_password_reset_email(to_email, first_name, reset_url):
    subject = "Reset your CourtSide Club password"
    body = f"""
    Hi {first_name},
    We received a request to reset your password. Click the button below to proceed:
    {reset_url}
    If you did not request this, please ignore this email.
    """
    # Use SendGrid or your email service to send with rich HTML version
    send_email(to_email, subject, body, html_content=render_reset_email_html(first_name, reset_url))
4. Frontend Forms
reset_password_request.html: Email input form with instructions
reset_password_confirm.html: New password input with confirm and strength checks
Both forms styled consistently with CourtSide Club’s brand and mobile-friendly design.
5. Additional Recommendations
Add rate limiting on the reset request endpoint to prevent abuse
Add token invalidation if user changes password or after expiration
Log reset attempts for auditing
Notify user upon successful password reset